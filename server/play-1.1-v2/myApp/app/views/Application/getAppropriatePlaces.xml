<?xml version="1.0"?>
<places total="${places.length}" limit="${limit}" offset="${offset}">
	{%
		for (int i=0;i<places.length;i++) {
	%}
	<place>
		<name>${places[i].getName()}</name>
		<address>${places[i].getAddress() }</address>
	</place>
	{%
		}
	%}
	
</places>